function [ Iinp ] = G7_inpainting_color( Im, mask )
lambda = 1/2;
delta_t = 1/4;
%Im = f
dim = size(Im);

p_n = zeros(3, 2, dim(1), dim(2));
while 1
    
    for i = 1:3
        p_n1 = (p_n(i, :, :, :) + lambda_t )/( );
    end   
    
end    
end

